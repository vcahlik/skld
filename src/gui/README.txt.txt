1) Build dependencies: packages "client", "representations" in the parent directory (run "mvn clean install" in their directories)
2) Build using "mvn clean install"
3) Start the server on "localhost:8080"
3) Run with "mvn exec:java"